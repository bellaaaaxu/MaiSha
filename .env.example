VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 仅用于 dev 侧批量图标生成脚本 scripts/generate-item-icons.mjs，客户端/App 不使用。
# 把真实值写进 .env（已 gitignore）。运行：node --env-file=.env scripts/generate-item-icons.mjs --limit 3
GEMINI_API_KEY=

# Sentry 错误监控（仅生产构建初始化；本地留空即可）。真实 DSN 配在 Cloudflare 构建环境变量。
VITE_SENTRY_DSN=
